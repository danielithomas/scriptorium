# Chatterbox TTS Server — NVIDIA GPU Dependencies (CUDA 12.1)
# Overrides CPU torch packages with CUDA-enabled versions
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# All other dependencies are installed via requirements.txt
