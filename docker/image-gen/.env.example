# Path to OpenVINO-converted models
MODELS_PATH=/path/to/your/models

# OpenVINO inference device: CPU, GPU (Intel iGPU/Arc), or AUTO
OV_DEVICE=CPU

# Model loaded at startup: sd15, dreamshaper, sdxl-turbo
DEFAULT_MODEL=sd15
